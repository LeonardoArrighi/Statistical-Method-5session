airquality
df <- airquality
boxplot(df$Ozone)
T_i = c(243, 14, 121, 63, 45, 407, 34)
T_i[1]
lambda <- 1/(2 * sum(T_i))
lambda
lambda <- 1/(2 * sum(a_i))
a_i = c(1,1)
lambda <- 1/(2 * sum(a_i))
a_i
lambda <- 1/(2 * sum(a_i))
lambda
T_i = c(243, 14, 121, 63, 45, 407, 34)
lambda <- 1/(2 * sum(T_i))
# given values for T_i
T_i = c(243, 14, 121, 63, 45, 407, 34)
# compute estimation
lambda <- 1/(2 * sum(T_i))
# compute estimation
lambda_hat <- length(T_i)/(2 * sum(T_i^2))
# given values for T_i
T_i = c(243, 14, 121, 63, 45, 407, 34)
# compute estimation
lambda_hat <- length(T_i)/(2 * sum(T_i^2))
# compute likelihood
log_likelihood <- function(data, param){
length(data) * log(sqrt(4 * param / pi)) - param * sum(data^2)
}
l_0 <- log_likelihood(T_i, 10^(-4))
# compute loglikelihood
log_likelihood <- function(x, lambda){
length(x) * log(sqrt(4 * lamda / pi)) - lambda * sum(x^2)
}
# compute estimation
lambda_hat <- length(T_i)/(2 * sum(T_i^2))
print(paste("MLE for lambda = ", lambda_hat))
l_0 <- log_likelihood(T_i, 10^(-4))
# compute loglikelihood
log_likelihood <- function(x, lambda){
length(x) * log(sqrt(4 * lambda / pi)) - lambda * sum(x^2)
}
# compute estimation
lambda_hat <- length(T_i)/(2 * sum(T_i^2))
print(paste("MLE for lambda = ", lambda_hat))
l_0 <- log_likelihood(T_i, 10^(-4))
l_0
# given values for T_i
T_i = c(243, 14, 121, 63, 45, 407, 34)
# compute loglikelihood
loglikelihood <- function(x, lambda){
length(x) * log(sqrt(4 * lambda / pi)) - lambda * sum(x^2)
}
# compute estimation
lambda_hat <- length(T_i)/(2 * sum(T_i^2))
print(paste("MLE for lambda = ", lambda_hat))
l_0 <- loglikelihood(T_i, 10^(-4))
l_hat <- loglikelihood(T_i, lambda_hat)
LR <- 2 * (l_hat - l_0)
LR
# given values for T_i
T_i = c(243, 14, 121, 63, 45, 407, 34)
# compute loglikelihood
loglikelihood <- function(x, lambda){
length(x) * log(sqrt(4 * lambda / pi)) - lambda * sum(x^2)
}
# compute estimation
lambda_hat <- length(T_i)/(2 * sum(T_i^2))
print(paste("MLE for lambda = ", lambda_hat))
l_0 <- loglikelihood(T_i, 10^(-4))
l_hat <- loglikelihood(T_i, lambda_hat)
LR <- 2 * (l_hat - l_0)
print(paste("Likelihood ratio test statistic is = ", LR))
p_lrt <- pchisq(LR, df=2, lower.tail = FALSE)
print(paste("p-value of Likelihood ratio test :", p_lrt))
help(pchisq)
# given values for T_i
T_i = c(243, 14, 121, 63, 45, 407, 34)
# compute loglikelihood
loglikelihood <- function(x, lambda){
length(x) * log(sqrt(4 * lambda / pi)) - lambda * sum(x^2)
}
# compute estimation
lambda_hat <- length(T_i)/(2 * sum(T_i^2))
print(paste("MLE for lambda = ", lambda_hat))
l_0 <- loglikelihood(T_i, 10^(-4))
l_hat <- loglikelihood(T_i, lambda_hat)
V <- 2 * (l_hat - l_0)
print(paste("Likelihood ratio test statistic is = ", V))
p_lrt <- pchisq(V, df=1, lower.tail = FALSE)
print(paste("p-value of likelihood ratio test is =", p_lrt))
